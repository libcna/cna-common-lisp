{
 "schema_version": 1,
 "description": "The deterministic projection from the selected XNA contract to Common Lisp. tools/api-compat/verify.py applies these rules to the live image; docs/common-lisp-mapping.md is the prose that explains them.",
 "naming": {
  "identifier": "A CLR identifier becomes a Lisp symbol by inserting a hyphen at each word boundary and lowercasing. Texture2D becomes texture-2d.",
  "reference_type_member": "<member>, in the type's package, as a generic function or reader.",
  "value_type_member": "<type>-<member>, in the type's package. The prefix is what keeps Rectangle's Left from colliding with Viewport's.",
  "static_class_member": "<class>-<member>, so that Mouse and GamePad can join Keyboard's namespace without collision.",
  "constructor": "make-instance for a reference type; make-<type> for a value type.",
  "enum_member": "a keyword named by the identifier rule; the exact value is preserved in the runtime table."
 },
 "namespace_packages": {
  "Microsoft.Xna.Framework": "microsoft.xna.framework",
  "Microsoft.Xna.Framework.Graphics": "microsoft.xna.framework.graphics",
  "Microsoft.Xna.Framework.Input": "microsoft.xna.framework.input"
 },
 "universal_not_applicable": [
  {
   "name": "ToString",
   "reason": "CLR object printing. A Common Lisp object prints through PRINT-OBJECT, which is a method on a standard generic function rather than a named member of the projected type."
  },
  {
   "name": "GetHashCode",
   "reason": "The CLR hash code is not observable through a Common Lisp API; hashing here is SXHASH over the projected value."
  },
  {
   "name": "Finalize",
   "reason": "CNA-Lisp has no finalizer that touches a native resource, by design. See docs/ownership-and-lifetimes.md."
  },
  {
   "name": "op_Inequality",
   "reason": "Common Lisp has no operator overloading. Inequality is the negation of the projected equality predicate."
  }
 ],
 "allowed_symbols": {
  "microsoft.xna.framework": [
   "keysp"
  ],
  "microsoft.xna.framework.graphics": [],
  "microsoft.xna.framework.input": [
   "keysp"
  ]
 },
 "types": {
  "Microsoft.Xna.Framework.Game": {
   "lisp_package": "microsoft.xna.framework",
   "lisp_name": "game",
   "lisp_kind": "class",
   "constructors": {
    "new()": "make-instance"
   },
   "member_overrides": {
    "OnExiting(Object,EventArgs)": {
     "lisp": "on-exiting",
     "generic": true,
     "reason": "The CLR event-handler pattern passes a sender and an argument object. A CLOS method on the game needs neither: the game is the receiver, and the exiting notification carries nothing else."
    }
   },
   "not_applicable": {
    "Dispose(Boolean)": "The protected dispose(bool disposing) pattern exists to tell a finalizer call from an explicit one. CNA-Lisp has no finalizer that touches a native resource, so there is nothing for the flag to distinguish."
   },
   "unimplemented": {
    "OnActivated(Object,EventArgs)": "Needs the event projection.",
    "OnDeactivated(Object,EventArgs)": "Needs the event projection.",
    "ShowMissingRequirementMessage(Exception)": "Needs the CLR exception projection and a platform message box."
   }
  },
  "Microsoft.Xna.Framework.GameTime": {
   "lisp_package": "microsoft.xna.framework",
   "lisp_name": "game-time",
   "lisp_kind": "class",
   "constructors": {
    "new()": "make-instance",
    "new(TimeSpan,TimeSpan,Boolean)": "make-instance",
    "new(TimeSpan,TimeSpan)": "make-instance"
   }
  },
  "Microsoft.Xna.Framework.GraphicsDeviceManager": {
   "lisp_package": "microsoft.xna.framework",
   "lisp_name": "graphics-device-manager",
   "lisp_kind": "class",
   "constructors": {
    "new(Game)": "make-instance"
   },
   "not_applicable": {
    "Dispose(Boolean)": "The protected dispose(bool) pattern; see Game.Dispose(Boolean)."
   }
  },
  "Microsoft.Xna.Framework.Color": {
   "lisp_package": "microsoft.xna.framework",
   "lisp_name": "color",
   "lisp_kind": "structure",
   "constructors": {
    "new(Int32,Int32,Int32)": "make-color",
    "new(Int32,Int32,Int32,Int32)": "make-color"
   },
   "member_overrides": {
    "Equals(Color)": {
     "lisp": "color-equal"
    },
    "op_Equality(Color,Color)": {
     "lisp": "color-equal"
    },
    "FromNonPremultiplied(Int32,Int32,Int32,Int32)": {
     "lisp": "color-from-non-premultiplied"
    },
    "Multiply(Color,Single)": {
     "lisp": "color-multiply"
    },
    "op_Multiply(Color,Single)": {
     "lisp": "color-multiply"
    }
   },
   "not_applicable": {
    "Equals(Object)": "There is no universal Object to compare against."
   },
   "unimplemented": {
    "new(Single,Single,Single)": "The float constructors need Vector3/Vector4 clamping semantics that are not projected yet.",
    "new(Single,Single,Single,Single)": "See the three-float constructor.",
    "new(Vector3)": "Vector3 is not projected.",
    "new(Vector4)": "Vector4 is not projected.",
    "FromNonPremultiplied(Vector4)": "Vector4 is not projected.",
    "ToVector3()": "Vector3 is not projected.",
    "ToVector4()": "Vector4 is not projected.",
    "Lerp(Color,Color,Single)": "Not implemented."
   },
   "overload_families": {
    "Equals": {
     "lisp": "color-equal",
     "reason": "Common Lisp has no operator overloading; equality projects to a named predicate."
    },
    "Multiply": {
     "lisp": "color-multiply",
     "reason": "One projection; the operator and the named method are the same operation."
    }
   },
   "predefined_value_properties": true,
   "predefined_value_reason": "XNA's 141 predefined colours are static properties on a value type. They are reached here by keyword through PREDEFINED-COLOR, which the generated table makes exhaustive; the thirteen with a named function of their own are declared extensions. A Lisp constant would be wrong for all of them, because Color is mutable and every read of the original is a copy."
  },
  "Microsoft.Xna.Framework.Point": {
   "lisp_package": "microsoft.xna.framework",
   "lisp_name": "point",
   "lisp_kind": "structure",
   "constructors": {
    "new(Int32,Int32)": "make-point"
   },
   "member_overrides": {
    "Equals(Point)": {
     "lisp": "point-equal"
    },
    "op_Equality(Point,Point)": {
     "lisp": "point-equal"
    }
   },
   "not_applicable": {
    "Equals(Object)": "There is no universal Object to compare against; the typed overload is the projection."
   },
   "overload_families": {
    "Equals": {
     "lisp": "point-equal",
     "reason": "Common Lisp has no operator overloading; equality projects to a named predicate."
    }
   }
  },
  "Microsoft.Xna.Framework.Rectangle": {
   "lisp_package": "microsoft.xna.framework",
   "lisp_name": "rectangle",
   "lisp_kind": "structure",
   "constructors": {
    "new(Int32,Int32,Int32,Int32)": "make-rectangle"
   },
   "member_overrides": {
    "Contains(Int32,Int32)": {
     "lisp": "rectangle-contains-coordinates",
     "reason": "Three arguments cannot share a congruent generic function with the two-argument overloads."
    },
    "Contains(Point)": {
     "lisp": "rectangle-contains",
     "generic": true
    },
    "Contains(Rectangle)": {
     "lisp": "rectangle-contains",
     "generic": true
    },
    "Equals(Rectangle)": {
     "lisp": "rectangle-equal"
    },
    "op_Equality(Rectangle,Rectangle)": {
     "lisp": "rectangle-equal"
    },
    "Offset(Int32,Int32)": {
     "lisp": "rectangle-offset"
    },
    "Intersects(Rectangle)": {
     "lisp": "rectangle-intersects"
    }
   },
   "not_applicable": {
    "Equals(Object)": "There is no universal Object to compare against.",
    "Contains(Point&,Boolean&)": "A by-reference overload of a pure computation. It exists in XNA so a caller can avoid copying a value type into a call and can write the answer into storage it already has; the value it computes is the by-value overload's. Common Lisp passes a reference already, so the by-value form is the whole contract, and projecting the ref form would be a second name for one operation.",
    "Contains(Rectangle&,Boolean&)": "A by-reference overload of a pure computation. It exists in XNA so a caller can avoid copying a value type into a call and can write the answer into storage it already has; the value it computes is the by-value overload's. Common Lisp passes a reference already, so the by-value form is the whole contract, and projecting the ref form would be a second name for one operation.",
    "Intersects(Rectangle&,Boolean&)": "A by-reference overload of a pure computation. It exists in XNA so a caller can avoid copying a value type into a call and can write the answer into storage it already has; the value it computes is the by-value overload's. Common Lisp passes a reference already, so the by-value form is the whole contract, and projecting the ref form would be a second name for one operation.",
    "Intersect(Rectangle&,Rectangle&,Rectangle&)": "A by-reference overload of a pure computation. It exists in XNA so a caller can avoid copying a value type into a call and can write the answer into storage it already has; the value it computes is the by-value overload's. Common Lisp passes a reference already, so the by-value form is the whole contract, and projecting the ref form would be a second name for one operation.",
    "Union(Rectangle&,Rectangle&,Rectangle&)": "A by-reference overload of a pure computation. It exists in XNA so a caller can avoid copying a value type into a call and can write the answer into storage it already has; the value it computes is the by-value overload's. Common Lisp passes a reference already, so the by-value form is the whole contract, and projecting the ref form would be a second name for one operation."
   },
   "unimplemented": {
    "Offset(Point)": "The Point overload is not projected; the coordinate one is.",
    "Intersect(Rectangle,Rectangle)": "Not implemented.",
    "Union(Rectangle,Rectangle)": "Not implemented."
   },
   "overload_families": {
    "Contains": {
     "lisp": "rectangle-contains and rectangle-contains-coordinates",
     "reason": "The two-argument overloads share a generic function; the three-argument one cannot."
    },
    "Equals": {
     "lisp": "rectangle-equal",
     "reason": "Common Lisp has no operator overloading; equality projects to a named predicate."
    }
   },
   "by_reference_reason": "A by-reference overload of a pure computation. It exists in XNA so a caller can avoid copying a value type into a call and can write the answer into storage it already has; the value it computes is the by-value overload's. Common Lisp passes a reference already, so the by-value form is the whole contract, and projecting the ref form would be a second name for one operation."
  },
  "Microsoft.Xna.Framework.Vector2": {
   "lisp_package": "microsoft.xna.framework",
   "lisp_name": "vector2",
   "lisp_kind": "structure",
   "constructors": {
    "new(Single,Single)": "make-vector2",
    "new(Single)": "make-vector2"
   },
   "member_overrides": {
    "Equals(Vector2)": {
     "lisp": "vector2-equal"
    },
    "op_Equality(Vector2,Vector2)": {
     "lisp": "vector2-equal"
    },
    "op_Addition(Vector2,Vector2)": {
     "lisp": "vector2-add"
    },
    "op_Subtraction(Vector2,Vector2)": {
     "lisp": "vector2-subtract"
    },
    "op_UnaryNegation(Vector2)": {
     "lisp": "vector2-negate"
    },
    "op_Multiply(Vector2,Single)": {
     "lisp": "vector2-multiply",
     "generic": true
    },
    "op_Multiply(Single,Vector2)": {
     "lisp": "vector2-multiply",
     "generic": true
    },
    "op_Multiply(Vector2,Vector2)": {
     "lisp": "vector2-multiply",
     "generic": true
    },
    "op_Division(Vector2,Single)": {
     "lisp": "vector2-divide",
     "generic": true
    },
    "op_Division(Vector2,Vector2)": {
     "lisp": "vector2-divide",
     "generic": true
    },
    "Add(Vector2,Vector2)": {
     "lisp": "vector2-add"
    },
    "Subtract(Vector2,Vector2)": {
     "lisp": "vector2-subtract"
    },
    "Negate(Vector2)": {
     "lisp": "vector2-negate"
    },
    "Multiply(Vector2,Single)": {
     "lisp": "vector2-multiply",
     "generic": true
    },
    "Multiply(Vector2,Vector2)": {
     "lisp": "vector2-multiply",
     "generic": true
    },
    "Divide(Vector2,Single)": {
     "lisp": "vector2-divide",
     "generic": true
    },
    "Divide(Vector2,Vector2)": {
     "lisp": "vector2-divide",
     "generic": true
    },
    "Distance(Vector2,Vector2)": {
     "lisp": "vector2-distance"
    },
    "DistanceSquared(Vector2,Vector2)": {
     "lisp": "vector2-distance-squared"
    },
    "Dot(Vector2,Vector2)": {
     "lisp": "vector2-dot"
    },
    "Normalize()": {
     "lisp": "vector2-normalize"
    },
    "Length()": {
     "lisp": "vector2-length"
    },
    "LengthSquared()": {
     "lisp": "vector2-length-squared"
    },
    "Normalize(Vector2)": {
     "lisp": "vector2-normalized",
     "reason": "The instance Normalize mutates and the static one answers a new vector."
    },
    "Reflect(Vector2,Vector2)": {
     "lisp": "vector2-reflect"
    },
    "Min(Vector2,Vector2)": {
     "lisp": "vector2-min"
    },
    "Max(Vector2,Vector2)": {
     "lisp": "vector2-max"
    },
    "Clamp(Vector2,Vector2,Vector2)": {
     "lisp": "vector2-clamp"
    },
    "Lerp(Vector2,Vector2,Single)": {
     "lisp": "vector2-lerp"
    },
    "SmoothStep(Vector2,Vector2,Single)": {
     "lisp": "vector2-smooth-step"
    },
    "Barycentric(Vector2,Vector2,Vector2,Single,Single)": {
     "lisp": "vector2-barycentric"
    },
    "CatmullRom(Vector2,Vector2,Vector2,Vector2,Single)": {
     "lisp": "vector2-catmull-rom"
    },
    "Hermite(Vector2,Vector2,Vector2,Vector2,Single)": {
     "lisp": "vector2-hermite"
    },
    "Transform(Vector2,Matrix)": {
     "lisp": "vector2-transform",
     "generic": true
    },
    "Transform(Vector2,Quaternion)": {
     "lisp": "vector2-transform",
     "generic": true
    },
    "TransformNormal(Vector2,Matrix)": {
     "lisp": "vector2-transform-normal"
    },
    "Transform(Vector2[],Matrix&,Vector2[])": {
     "lisp": "vector2-transform-array",
     "reason": "The array overloads: SOURCE-INDEX, DESTINATION-INDEX and LENGTH express the windowed form, and the whole-array form is the same function with none of them."
    },
    "Transform(Vector2[],Int32,Matrix&,Vector2[],Int32,Int32)": {
     "lisp": "vector2-transform-array",
     "reason": "The array overloads: SOURCE-INDEX, DESTINATION-INDEX and LENGTH express the windowed form, and the whole-array form is the same function with none of them."
    },
    "Transform(Vector2[],Quaternion&,Vector2[])": {
     "lisp": "vector2-transform-array",
     "reason": "The array overloads: SOURCE-INDEX, DESTINATION-INDEX and LENGTH express the windowed form, and the whole-array form is the same function with none of them."
    },
    "Transform(Vector2[],Int32,Quaternion&,Vector2[],Int32,Int32)": {
     "lisp": "vector2-transform-array",
     "reason": "The array overloads: SOURCE-INDEX, DESTINATION-INDEX and LENGTH express the windowed form, and the whole-array form is the same function with none of them."
    },
    "TransformNormal(Vector2[],Matrix&,Vector2[])": {
     "lisp": "vector2-transform-normal-array",
     "reason": "The array overloads: SOURCE-INDEX, DESTINATION-INDEX and LENGTH express the windowed form, and the whole-array form is the same function with none of them."
    },
    "TransformNormal(Vector2[],Int32,Matrix&,Vector2[],Int32,Int32)": {
     "lisp": "vector2-transform-normal-array",
     "reason": "The array overloads: SOURCE-INDEX, DESTINATION-INDEX and LENGTH express the windowed form, and the whole-array form is the same function with none of them."
    }
   },
   "not_applicable": {
    "Equals(Object)": "There is no universal Object to compare against.",
    "Add(Vector2&,Vector2&,Vector2&)": "A by-reference overload of a pure computation. It exists in XNA so a caller can avoid copying a value type into a call and can write the answer into storage it already has; the value it computes is the by-value overload's. Common Lisp passes a reference already, so the by-value form is the whole contract, and projecting the ref form would be a second name for one operation.",
    "Subtract(Vector2&,Vector2&,Vector2&)": "A by-reference overload of a pure computation. It exists in XNA so a caller can avoid copying a value type into a call and can write the answer into storage it already has; the value it computes is the by-value overload's. Common Lisp passes a reference already, so the by-value form is the whole contract, and projecting the ref form would be a second name for one operation.",
    "Negate(Vector2&,Vector2&)": "A by-reference overload of a pure computation. It exists in XNA so a caller can avoid copying a value type into a call and can write the answer into storage it already has; the value it computes is the by-value overload's. Common Lisp passes a reference already, so the by-value form is the whole contract, and projecting the ref form would be a second name for one operation.",
    "Multiply(Vector2&,Single,Vector2&)": "A by-reference overload of a pure computation. It exists in XNA so a caller can avoid copying a value type into a call and can write the answer into storage it already has; the value it computes is the by-value overload's. Common Lisp passes a reference already, so the by-value form is the whole contract, and projecting the ref form would be a second name for one operation.",
    "Multiply(Vector2&,Vector2&,Vector2&)": "A by-reference overload of a pure computation. It exists in XNA so a caller can avoid copying a value type into a call and can write the answer into storage it already has; the value it computes is the by-value overload's. Common Lisp passes a reference already, so the by-value form is the whole contract, and projecting the ref form would be a second name for one operation.",
    "Divide(Vector2&,Single,Vector2&)": "A by-reference overload of a pure computation. It exists in XNA so a caller can avoid copying a value type into a call and can write the answer into storage it already has; the value it computes is the by-value overload's. Common Lisp passes a reference already, so the by-value form is the whole contract, and projecting the ref form would be a second name for one operation.",
    "Divide(Vector2&,Vector2&,Vector2&)": "A by-reference overload of a pure computation. It exists in XNA so a caller can avoid copying a value type into a call and can write the answer into storage it already has; the value it computes is the by-value overload's. Common Lisp passes a reference already, so the by-value form is the whole contract, and projecting the ref form would be a second name for one operation.",
    "Distance(Vector2&,Vector2&,Single&)": "A by-reference overload of a pure computation. It exists in XNA so a caller can avoid copying a value type into a call and can write the answer into storage it already has; the value it computes is the by-value overload's. Common Lisp passes a reference already, so the by-value form is the whole contract, and projecting the ref form would be a second name for one operation.",
    "DistanceSquared(Vector2&,Vector2&,Single&)": "A by-reference overload of a pure computation. It exists in XNA so a caller can avoid copying a value type into a call and can write the answer into storage it already has; the value it computes is the by-value overload's. Common Lisp passes a reference already, so the by-value form is the whole contract, and projecting the ref form would be a second name for one operation.",
    "Dot(Vector2&,Vector2&,Single&)": "A by-reference overload of a pure computation. It exists in XNA so a caller can avoid copying a value type into a call and can write the answer into storage it already has; the value it computes is the by-value overload's. Common Lisp passes a reference already, so the by-value form is the whole contract, and projecting the ref form would be a second name for one operation.",
    "Normalize(Vector2&,Vector2&)": "A by-reference overload of a pure computation. It exists in XNA so a caller can avoid copying a value type into a call and can write the answer into storage it already has; the value it computes is the by-value overload's. Common Lisp passes a reference already, so the by-value form is the whole contract, and projecting the ref form would be a second name for one operation.",
    "Transform(Vector2&,Matrix&,Vector2&)": "A by-reference overload of a pure computation. It exists in XNA so a caller can avoid copying a value type into a call and can write the answer into storage it already has; the value it computes is the by-value overload's. Common Lisp passes a reference already, so the by-value form is the whole contract, and projecting the ref form would be a second name for one operation.",
    "TransformNormal(Vector2&,Matrix&,Vector2&)": "A by-reference overload of a pure computation. It exists in XNA so a caller can avoid copying a value type into a call and can write the answer into storage it already has; the value it computes is the by-value overload's. Common Lisp passes a reference already, so the by-value form is the whole contract, and projecting the ref form would be a second name for one operation.",
    "Transform(Vector2&,Quaternion&,Vector2&)": "A by-reference overload of a pure computation. It exists in XNA so a caller can avoid copying a value type into a call and can write the answer into storage it already has; the value it computes is the by-value overload's. Common Lisp passes a reference already, so the by-value form is the whole contract, and projecting the ref form would be a second name for one operation."
   },
   "unimplemented": {},
   "overload_families": {
    "Equals": {
     "lisp": "vector2-equal",
     "reason": "Common Lisp has no operator overloading; equality projects to a named predicate."
    },
    "Multiply": {
     "lisp": "vector2-multiply",
     "reason": "One generic function with methods on REAL and on VECTOR2: congruent lambda lists, genuine type dispatch."
    },
    "Divide": {
     "lisp": "vector2-divide",
     "reason": "As Multiply."
    },
    "op_Multiply": {
     "lisp": "vector2-multiply",
     "reason": "As Multiply."
    },
    "op_Division": {
     "lisp": "vector2-divide",
     "reason": "As Multiply."
    },
    "Normalize": {
     "lisp": "vector2-normalize and vector2-normalized",
     "reason": "The instance form mutates; the static form does not."
    },
    "Transform": {
     "lisp": "vector2-transform and vector2-transform-array",
     "reason": "The three-argument whole-array overload and the six-argument windowed one differ only by trailing parameters, so one function with :SOURCE-INDEX, :DESTINATION-INDEX and :LENGTH expresses both. The Matrix and Quaternion forms are methods of the element transform, dispatching on the second argument."
    },
    "TransformNormal": {
     "lisp": "vector2-transform-normal and vector2-transform-normal-array",
     "reason": "The three-argument whole-array overload and the six-argument windowed one differ only by trailing parameters, so one function with :SOURCE-INDEX, :DESTINATION-INDEX and :LENGTH expresses both. The Matrix and Quaternion forms are methods of the element transform, dispatching on the second argument."
    }
   },
   "by_reference_reason": "A by-reference overload of a pure computation. It exists in XNA so a caller can avoid copying a value type into a call and can write the answer into storage it already has; the value it computes is the by-value overload's. Common Lisp passes a reference already, so the by-value form is the whole contract, and projecting the ref form would be a second name for one operation."
  },
  "Microsoft.Xna.Framework.PlayerIndex": {
   "lisp_package": "microsoft.xna.framework",
   "lisp_name": "player-index",
   "lisp_kind": "enum"
  },
  "Microsoft.Xna.Framework.Graphics.GraphicsResource": {
   "lisp_package": "microsoft.xna.framework.graphics",
   "lisp_name": "graphics-resource",
   "lisp_kind": "class",
   "status": "missing",
   "reason": "GraphicsResource's disposal contract, its Name, Tag and Disposing event need the event projection and the GraphicsDevice back-reference, neither of which is in Foundation 1. Texture2D and SpriteBatch carry CNA-Lisp's own disposal instead."
  },
  "Microsoft.Xna.Framework.Graphics.GraphicsDevice": {
   "lisp_package": "microsoft.xna.framework.graphics",
   "lisp_name": "graphics-device",
   "lisp_kind": "class",
   "constructors": {},
   "member_overrides": {
    "Clear(Color)": {
     "lisp": "clear",
     "generic": true
    },
    "Present()": {
     "lisp": "present",
     "generic": true
    },
    "Viewport": {
     "lisp": "viewport",
     "setf": false,
     "status": "partial",
     "reason": "The reader is present. The setter is externally blocked: cna_graphics_device_set_viewport takes CNA_Viewport (24 bytes) by value, which the System V AMD64 ABI classifies MEMORY and CFFI cannot pass without cffi-libffi. See docs/native-abi.md."
    }
   },
   "not_applicable": {
    "Dispose(Boolean)": "The protected dispose(bool) pattern."
   },
   "unimplemented": {
    "Present(Rectangle],Rectangle],IntPtr)": "Needs Nullable<Rectangle> and IntPtr, neither of which is projected.",
    "Reset()": "Device reset is not projected.",
    "Reset(PresentationParameters)": "Needs PresentationParameters.",
    "Reset(PresentationParameters,GraphicsAdapter)": "Needs PresentationParameters and GraphicsAdapter.",
    "Clear(ClearOptions,Vector4,Single,Int32)": "Needs ClearOptions and Vector4.",
    "Clear(ClearOptions,Color,Single,Int32)": "Needs ClearOptions."
   },
   "overload_families": {
    "Present": {
     "lisp": "present",
     "reason": "Only the no-argument overload is projected."
    },
    "Clear": {
     "lisp": "clear",
     "reason": "Only Clear(Color) is projected."
    }
   }
  },
  "Microsoft.Xna.Framework.Graphics.Viewport": {
   "lisp_package": "microsoft.xna.framework.graphics",
   "lisp_name": "viewport",
   "lisp_kind": "structure",
   "constructors": {
    "new(Int32,Int32,Int32,Int32)": "make-viewport"
   },
   "unimplemented": {
    "new(Rectangle)": "The rectangle constructor is not projected; the four-integer one is.",
    "Project(Vector3,Matrix,Matrix,Matrix)": "Needs Vector3 and Matrix.",
    "Unproject(Vector3,Matrix,Matrix,Matrix)": "Needs Vector3 and Matrix."
   }
  },
  "Microsoft.Xna.Framework.Graphics.Texture": {
   "lisp_package": "microsoft.xna.framework.graphics",
   "lisp_name": "texture",
   "lisp_kind": "class",
   "member_overrides": {
    "Format": {
     "lisp": "format-of",
     "reason": "A member whose projected name would be a Common Lisp standard symbol takes an -of suffix: cl:format is inherited into this package."
    }
   }
  },
  "Microsoft.Xna.Framework.Graphics.Texture2D": {
   "lisp_package": "microsoft.xna.framework.graphics",
   "lisp_name": "texture-2d",
   "lisp_kind": "class",
   "expected_superclass": "microsoft.xna.framework.graphics:texture",
   "constructors": {},
   "not_applicable": {
    "Dispose(Boolean)": "The protected dispose(bool) pattern."
   },
   "unimplemented": {
    "new(GraphicsDevice,Int32,Int32)": "Creating an empty texture is not projected in this milestone.",
    "new(GraphicsDevice,Int32,Int32,Boolean,SurfaceFormat)": "See the three-argument constructor.",
    "FromStream(GraphicsDevice,Stream,Int32,Int32,Boolean)": "Needs the System.IO.Stream projection. TEXTURE-2D-FROM-PNG-BYTES and TEXTURE-2D-FROM-PNG-FILE are CNA-Lisp additions over the same CNA route, and are declared as extensions rather than claimed as this member.",
    "FromStream(GraphicsDevice,Stream)": "See the five-argument overload.",
    "SaveAsJpeg(Stream,Int32,Int32)": "Needs the Stream projection.",
    "SaveAsPng(Stream,Int32,Int32)": "Needs the Stream projection.",
    "SetData(!!0[])": "Pixel access is not projected in this milestone.",
    "SetData(!!0[],Int32,Int32)": "Pixel access is not projected.",
    "SetData(Int32,Rectangle],!!0[],Int32,Int32)": "Pixel access is not projected.",
    "GetData(!!0[])": "Pixel access is not projected.",
    "GetData(!!0[],Int32,Int32)": "Pixel access is not projected.",
    "GetData(Int32,Rectangle],!!0[],Int32,Int32)": "Pixel access is not projected."
   }
  },
  "Microsoft.Xna.Framework.Graphics.SpriteBatch": {
   "lisp_package": "microsoft.xna.framework.graphics",
   "lisp_name": "sprite-batch",
   "lisp_kind": "class",
   "constructors": {
    "new(GraphicsDevice)": "make-instance"
   },
   "member_overrides": {
    "Draw(Texture2D,Vector2,Color)": {
     "lisp": "draw-texture",
     "generic": true
    },
    "Draw(Texture2D,Vector2,Rectangle],Color)": {
     "lisp": "draw-texture",
     "generic": true
    },
    "Draw(Texture2D,Vector2,Rectangle],Color,Single,Vector2,Single,SpriteEffects,Single)": {
     "lisp": "draw-texture",
     "generic": true
    },
    "Draw(Texture2D,Vector2,Rectangle],Color,Single,Vector2,Vector2,SpriteEffects,Single)": {
     "lisp": "draw-texture",
     "generic": true
    },
    "Draw(Texture2D,Rectangle,Color)": {
     "lisp": "draw-texture",
     "generic": true
    },
    "Draw(Texture2D,Rectangle,Rectangle],Color)": {
     "lisp": "draw-texture",
     "generic": true
    },
    "Draw(Texture2D,Rectangle,Rectangle],Color,Single,Vector2,SpriteEffects,Single)": {
     "lisp": "draw-texture",
     "generic": true
    },
    "Begin()": {
     "lisp": "begin",
     "generic": true
    },
    "End()": {
     "lisp": "end",
     "generic": true
    }
   },
   "not_applicable": {
    "Dispose(Boolean)": "The protected dispose(bool) pattern."
   },
   "unimplemented": {
    "Begin(SpriteSortMode,BlendState)": "Needs BlendState.",
    "Begin(SpriteSortMode,BlendState,SamplerState,DepthStencilState,RasterizerState)": "Needs the four graphics state objects.",
    "Begin(SpriteSortMode,BlendState,SamplerState,DepthStencilState,RasterizerState,Effect)": "Needs the state objects and Effect.",
    "Begin(SpriteSortMode,BlendState,SamplerState,DepthStencilState,RasterizerState,Effect,Matrix)": "Needs the state objects, Effect and Matrix.",
    "DrawString(SpriteFont,String,Vector2,Color)": "Needs SpriteFont.",
    "DrawString(SpriteFont,StringBuilder,Vector2,Color)": "Needs SpriteFont and StringBuilder.",
    "DrawString(SpriteFont,String,Vector2,Color,Single,Vector2,Single,SpriteEffects,Single)": "Needs SpriteFont.",
    "DrawString(SpriteFont,StringBuilder,Vector2,Color,Single,Vector2,Single,SpriteEffects,Single)": "Needs SpriteFont and StringBuilder.",
    "DrawString(SpriteFont,String,Vector2,Color,Single,Vector2,Vector2,SpriteEffects,Single)": "Needs SpriteFont.",
    "DrawString(SpriteFont,StringBuilder,Vector2,Color,Single,Vector2,Vector2,SpriteEffects,Single)": "Needs SpriteFont and StringBuilder."
   },
   "overload_families": {
    "Begin": {
     "lisp": "begin",
     "reason": "Only the no-argument overload is projected; the state-bearing ones need types this milestone does not have."
    },
    "Draw": {
     "lisp": "draw-texture",
     "reason": "All seven texture overloads are one generic function with keyword arguments. They differ only in which optional parameters are present and in whether the destination is a position or a rectangle, and DRAW-TEXTURE refuses both at once rather than guessing. Text drawing will be a separate generic function when SpriteFont arrives, because one DRAW would then be ambiguous."
    }
   }
  },
  "Microsoft.Xna.Framework.Graphics.SpriteSortMode": {
   "lisp_package": "microsoft.xna.framework.graphics",
   "lisp_name": "sprite-sort-mode",
   "lisp_kind": "enum"
  },
  "Microsoft.Xna.Framework.Graphics.SpriteEffects": {
   "lisp_package": "microsoft.xna.framework.graphics",
   "lisp_name": "sprite-effects",
   "lisp_kind": "enum"
  },
  "Microsoft.Xna.Framework.Graphics.SurfaceFormat": {
   "lisp_package": "microsoft.xna.framework.graphics",
   "lisp_name": "surface-format",
   "lisp_kind": "enum",
   "extra_enum_members": [
    "color-bgra-ext",
    "color-srgb-ext",
    "dxt5-srgb-ext",
    "bc7-ext",
    "bc7-srgb-ext",
    "byte-ext",
    "ushort-ext"
   ],
   "extra_enum_reason": "CNA's own renderer-specific surface formats. They are not XNA members; a texture CNA created can report one, so the projection must be able to name it, and a consumer never has to supply one."
  },
  "Microsoft.Xna.Framework.Input.Keyboard": {
   "lisp_package": "microsoft.xna.framework.input",
   "lisp_name": "keyboard",
   "lisp_kind": "static",
   "member_overrides": {
    "GetState()": {
     "lisp": "keyboard-get-state"
    },
    "GetState(PlayerIndex)": {
     "lisp": "keyboard-get-state"
    }
   },
   "overload_families": {
    "GetState": {
     "lisp": "keyboard-get-state",
     "reason": "The player-slot overload is an optional argument: the lambda lists differ only by a trailing parameter, so one function expresses both without ambiguity."
    }
   }
  },
  "Microsoft.Xna.Framework.Input.KeyboardState": {
   "lisp_package": "microsoft.xna.framework.input",
   "lisp_name": "keyboard-state",
   "lisp_kind": "structure",
   "constructors": {
    "new(Keys[])": "make-keyboard-state"
   },
   "member_overrides": {
    "IsKeyDown(Keys)": {
     "lisp": "is-key-down",
     "reason": "The value-type prefix is dropped here: the three query methods read as sentences at the call site and their names are unique in this namespace. Declared rather than assumed."
    },
    "IsKeyUp(Keys)": {
     "lisp": "is-key-up",
     "reason": "See IsKeyDown."
    },
    "GetPressedKeys()": {
     "lisp": "get-pressed-keys",
     "reason": "See IsKeyDown."
    },
    "Item": {
     "lisp": "get-key-state",
     "setf": false,
     "reason": "The CLR indexer. Common Lisp has no indexer syntax, so it projects to a named reader."
    },
    "op_Equality(KeyboardState,KeyboardState)": {
     "lisp": "keyboard-state-equal"
    }
   },
   "not_applicable": {
    "Equals(Object)": "There is no universal Object to compare against."
   }
  },
  "Microsoft.Xna.Framework.Input.KeyState": {
   "lisp_package": "microsoft.xna.framework.input",
   "lisp_name": "key-state",
   "lisp_kind": "enum"
  },
  "Microsoft.Xna.Framework.Input.Keys": {
   "lisp_package": "microsoft.xna.framework.input",
   "lisp_name": "keys",
   "lisp_kind": "enum"
  },
  "Microsoft.Xna.Framework.MathHelper": {
   "lisp_package": "microsoft.xna.framework",
   "lisp_name": "math-helper",
   "lisp_kind": "static",
   "member_overrides": {
    "E": {
     "lisp": "+math-helper-e+",
     "kind": "constant"
    },
    "Log2E": {
     "lisp": "+math-helper-log2e+",
     "kind": "constant"
    },
    "Log10E": {
     "lisp": "+math-helper-log10e+",
     "kind": "constant"
    },
    "Pi": {
     "lisp": "+math-helper-pi+",
     "kind": "constant"
    },
    "TwoPi": {
     "lisp": "+math-helper-two-pi+",
     "kind": "constant"
    },
    "PiOver2": {
     "lisp": "+math-helper-pi-over2+",
     "kind": "constant"
    },
    "PiOver4": {
     "lisp": "+math-helper-pi-over4+",
     "kind": "constant"
    }
   },
   "constant_field_reason": "A constant field of a static class projects to a Common Lisp constant, which by convention wears earmuffs: +math-helper-pi+."
  },
  "Microsoft.Xna.Framework.Vector3": {
   "lisp_package": "microsoft.xna.framework",
   "lisp_name": "vector3",
   "lisp_kind": "structure",
   "constructors": {
    "new(Single,Single,Single)": "make-vector3",
    "new(Single)": "make-vector3",
    "new(Vector2,Single)": "make-vector3-from-vector2"
   },
   "member_overrides": {
    "Equals(Vector3)": {
     "lisp": "vector3-equal"
    },
    "op_Equality(Vector3,Vector3)": {
     "lisp": "vector3-equal"
    },
    "op_Addition(Vector3,Vector3)": {
     "lisp": "vector3-add"
    },
    "op_Subtraction(Vector3,Vector3)": {
     "lisp": "vector3-subtract"
    },
    "op_UnaryNegation(Vector3)": {
     "lisp": "vector3-negate"
    },
    "op_Multiply(Vector3,Single)": {
     "lisp": "vector3-multiply",
     "generic": true
    },
    "op_Multiply(Single,Vector3)": {
     "lisp": "vector3-multiply",
     "generic": true
    },
    "op_Multiply(Vector3,Vector3)": {
     "lisp": "vector3-multiply",
     "generic": true
    },
    "op_Division(Vector3,Single)": {
     "lisp": "vector3-divide",
     "generic": true
    },
    "op_Division(Vector3,Vector3)": {
     "lisp": "vector3-divide",
     "generic": true
    },
    "Add(Vector3,Vector3)": {
     "lisp": "vector3-add"
    },
    "Subtract(Vector3,Vector3)": {
     "lisp": "vector3-subtract"
    },
    "Negate(Vector3)": {
     "lisp": "vector3-negate"
    },
    "Multiply(Vector3,Single)": {
     "lisp": "vector3-multiply",
     "generic": true
    },
    "Multiply(Vector3,Vector3)": {
     "lisp": "vector3-multiply",
     "generic": true
    },
    "Divide(Vector3,Single)": {
     "lisp": "vector3-divide",
     "generic": true
    },
    "Divide(Vector3,Vector3)": {
     "lisp": "vector3-divide",
     "generic": true
    },
    "Distance(Vector3,Vector3)": {
     "lisp": "vector3-distance"
    },
    "DistanceSquared(Vector3,Vector3)": {
     "lisp": "vector3-distance-squared"
    },
    "Dot(Vector3,Vector3)": {
     "lisp": "vector3-dot"
    },
    "Min(Vector3,Vector3)": {
     "lisp": "vector3-min"
    },
    "Max(Vector3,Vector3)": {
     "lisp": "vector3-max"
    },
    "Clamp(Vector3,Vector3,Vector3)": {
     "lisp": "vector3-clamp"
    },
    "Lerp(Vector3,Vector3,Single)": {
     "lisp": "vector3-lerp"
    },
    "SmoothStep(Vector3,Vector3,Single)": {
     "lisp": "vector3-smooth-step"
    },
    "Barycentric(Vector3,Vector3,Vector3,Single,Single)": {
     "lisp": "vector3-barycentric"
    },
    "CatmullRom(Vector3,Vector3,Vector3,Vector3,Single)": {
     "lisp": "vector3-catmull-rom"
    },
    "Hermite(Vector3,Vector3,Vector3,Vector3,Single)": {
     "lisp": "vector3-hermite"
    },
    "Normalize()": {
     "lisp": "vector3-normalize"
    },
    "Normalize(Vector3)": {
     "lisp": "vector3-normalized",
     "reason": "The instance Normalize mutates and the static one answers a new vector. One Lisp function cannot be both without the caller having to know which it got, so the static overload takes the adjective."
    },
    "Length()": {
     "lisp": "vector3-length"
    },
    "LengthSquared()": {
     "lisp": "vector3-length-squared"
    },
    "Cross(Vector3,Vector3)": {
     "lisp": "vector3-cross"
    },
    "Reflect(Vector3,Vector3)": {
     "lisp": "vector3-reflect"
    },
    "Transform(Vector3,Matrix)": {
     "lisp": "vector3-transform",
     "generic": true
    },
    "Transform(Vector3,Quaternion)": {
     "lisp": "vector3-transform",
     "generic": true
    },
    "TransformNormal(Vector3,Matrix)": {
     "lisp": "vector3-transform-normal"
    },
    "Transform(Vector3[],Matrix&,Vector3[])": {
     "lisp": "vector3-transform-array",
     "reason": "The array overloads: SOURCE-INDEX, DESTINATION-INDEX and LENGTH express the windowed form, and the whole-array form is the same function with none of them."
    },
    "Transform(Vector3[],Int32,Matrix&,Vector3[],Int32,Int32)": {
     "lisp": "vector3-transform-array",
     "reason": "The array overloads: SOURCE-INDEX, DESTINATION-INDEX and LENGTH express the windowed form, and the whole-array form is the same function with none of them."
    },
    "Transform(Vector3[],Quaternion&,Vector3[])": {
     "lisp": "vector3-transform-array",
     "reason": "The array overloads: SOURCE-INDEX, DESTINATION-INDEX and LENGTH express the windowed form, and the whole-array form is the same function with none of them."
    },
    "Transform(Vector3[],Int32,Quaternion&,Vector3[],Int32,Int32)": {
     "lisp": "vector3-transform-array",
     "reason": "The array overloads: SOURCE-INDEX, DESTINATION-INDEX and LENGTH express the windowed form, and the whole-array form is the same function with none of them."
    },
    "TransformNormal(Vector3[],Matrix&,Vector3[])": {
     "lisp": "vector3-transform-normal-array",
     "reason": "The array overloads: SOURCE-INDEX, DESTINATION-INDEX and LENGTH express the windowed form, and the whole-array form is the same function with none of them."
    },
    "TransformNormal(Vector3[],Int32,Matrix&,Vector3[],Int32,Int32)": {
     "lisp": "vector3-transform-normal-array",
     "reason": "The array overloads: SOURCE-INDEX, DESTINATION-INDEX and LENGTH express the windowed form, and the whole-array form is the same function with none of them."
    }
   },
   "not_applicable": {
    "Equals(Object)": "There is no universal Object to compare against.",
    "Distance(Vector3&,Vector3&,Single&)": "A by-reference overload of a pure computation. It exists in XNA so a caller can avoid copying a value type into a call and can write the answer into storage it already has; the value it computes is the by-value overload's. Common Lisp passes a reference already, so the by-value form is the whole contract, and projecting the ref form would be a second name for one operation.",
    "DistanceSquared(Vector3&,Vector3&,Single&)": "A by-reference overload of a pure computation. It exists in XNA so a caller can avoid copying a value type into a call and can write the answer into storage it already has; the value it computes is the by-value overload's. Common Lisp passes a reference already, so the by-value form is the whole contract, and projecting the ref form would be a second name for one operation.",
    "Dot(Vector3&,Vector3&,Single&)": "A by-reference overload of a pure computation. It exists in XNA so a caller can avoid copying a value type into a call and can write the answer into storage it already has; the value it computes is the by-value overload's. Common Lisp passes a reference already, so the by-value form is the whole contract, and projecting the ref form would be a second name for one operation.",
    "Normalize(Vector3&,Vector3&)": "A by-reference overload of a pure computation. It exists in XNA so a caller can avoid copying a value type into a call and can write the answer into storage it already has; the value it computes is the by-value overload's. Common Lisp passes a reference already, so the by-value form is the whole contract, and projecting the ref form would be a second name for one operation.",
    "Cross(Vector3&,Vector3&,Vector3&)": "A by-reference overload of a pure computation. It exists in XNA so a caller can avoid copying a value type into a call and can write the answer into storage it already has; the value it computes is the by-value overload's. Common Lisp passes a reference already, so the by-value form is the whole contract, and projecting the ref form would be a second name for one operation.",
    "Reflect(Vector3&,Vector3&,Vector3&)": "A by-reference overload of a pure computation. It exists in XNA so a caller can avoid copying a value type into a call and can write the answer into storage it already has; the value it computes is the by-value overload's. Common Lisp passes a reference already, so the by-value form is the whole contract, and projecting the ref form would be a second name for one operation.",
    "Min(Vector3&,Vector3&,Vector3&)": "A by-reference overload of a pure computation. It exists in XNA so a caller can avoid copying a value type into a call and can write the answer into storage it already has; the value it computes is the by-value overload's. Common Lisp passes a reference already, so the by-value form is the whole contract, and projecting the ref form would be a second name for one operation.",
    "Max(Vector3&,Vector3&,Vector3&)": "A by-reference overload of a pure computation. It exists in XNA so a caller can avoid copying a value type into a call and can write the answer into storage it already has; the value it computes is the by-value overload's. Common Lisp passes a reference already, so the by-value form is the whole contract, and projecting the ref form would be a second name for one operation.",
    "Clamp(Vector3&,Vector3&,Vector3&,Vector3&)": "A by-reference overload of a pure computation. It exists in XNA so a caller can avoid copying a value type into a call and can write the answer into storage it already has; the value it computes is the by-value overload's. Common Lisp passes a reference already, so the by-value form is the whole contract, and projecting the ref form would be a second name for one operation.",
    "Lerp(Vector3&,Vector3&,Single,Vector3&)": "A by-reference overload of a pure computation. It exists in XNA so a caller can avoid copying a value type into a call and can write the answer into storage it already has; the value it computes is the by-value overload's. Common Lisp passes a reference already, so the by-value form is the whole contract, and projecting the ref form would be a second name for one operation.",
    "SmoothStep(Vector3&,Vector3&,Single,Vector3&)": "A by-reference overload of a pure computation. It exists in XNA so a caller can avoid copying a value type into a call and can write the answer into storage it already has; the value it computes is the by-value overload's. Common Lisp passes a reference already, so the by-value form is the whole contract, and projecting the ref form would be a second name for one operation.",
    "Barycentric(Vector3&,Vector3&,Vector3&,Single,Single,Vector3&)": "A by-reference overload of a pure computation. It exists in XNA so a caller can avoid copying a value type into a call and can write the answer into storage it already has; the value it computes is the by-value overload's. Common Lisp passes a reference already, so the by-value form is the whole contract, and projecting the ref form would be a second name for one operation.",
    "CatmullRom(Vector3&,Vector3&,Vector3&,Vector3&,Single,Vector3&)": "A by-reference overload of a pure computation. It exists in XNA so a caller can avoid copying a value type into a call and can write the answer into storage it already has; the value it computes is the by-value overload's. Common Lisp passes a reference already, so the by-value form is the whole contract, and projecting the ref form would be a second name for one operation.",
    "Hermite(Vector3&,Vector3&,Vector3&,Vector3&,Single,Vector3&)": "A by-reference overload of a pure computation. It exists in XNA so a caller can avoid copying a value type into a call and can write the answer into storage it already has; the value it computes is the by-value overload's. Common Lisp passes a reference already, so the by-value form is the whole contract, and projecting the ref form would be a second name for one operation.",
    "Negate(Vector3&,Vector3&)": "A by-reference overload of a pure computation. It exists in XNA so a caller can avoid copying a value type into a call and can write the answer into storage it already has; the value it computes is the by-value overload's. Common Lisp passes a reference already, so the by-value form is the whole contract, and projecting the ref form would be a second name for one operation.",
    "Add(Vector3&,Vector3&,Vector3&)": "A by-reference overload of a pure computation. It exists in XNA so a caller can avoid copying a value type into a call and can write the answer into storage it already has; the value it computes is the by-value overload's. Common Lisp passes a reference already, so the by-value form is the whole contract, and projecting the ref form would be a second name for one operation.",
    "Subtract(Vector3&,Vector3&,Vector3&)": "A by-reference overload of a pure computation. It exists in XNA so a caller can avoid copying a value type into a call and can write the answer into storage it already has; the value it computes is the by-value overload's. Common Lisp passes a reference already, so the by-value form is the whole contract, and projecting the ref form would be a second name for one operation.",
    "Multiply(Vector3&,Vector3&,Vector3&)": "A by-reference overload of a pure computation. It exists in XNA so a caller can avoid copying a value type into a call and can write the answer into storage it already has; the value it computes is the by-value overload's. Common Lisp passes a reference already, so the by-value form is the whole contract, and projecting the ref form would be a second name for one operation.",
    "Multiply(Vector3&,Single,Vector3&)": "A by-reference overload of a pure computation. It exists in XNA so a caller can avoid copying a value type into a call and can write the answer into storage it already has; the value it computes is the by-value overload's. Common Lisp passes a reference already, so the by-value form is the whole contract, and projecting the ref form would be a second name for one operation.",
    "Divide(Vector3&,Vector3&,Vector3&)": "A by-reference overload of a pure computation. It exists in XNA so a caller can avoid copying a value type into a call and can write the answer into storage it already has; the value it computes is the by-value overload's. Common Lisp passes a reference already, so the by-value form is the whole contract, and projecting the ref form would be a second name for one operation.",
    "Divide(Vector3&,Single,Vector3&)": "A by-reference overload of a pure computation. It exists in XNA so a caller can avoid copying a value type into a call and can write the answer into storage it already has; the value it computes is the by-value overload's. Common Lisp passes a reference already, so the by-value form is the whole contract, and projecting the ref form would be a second name for one operation.",
    "Transform(Vector3&,Matrix&,Vector3&)": "A by-reference overload of a pure computation. It exists in XNA so a caller can avoid copying a value type into a call and can write the answer into storage it already has; the value it computes is the by-value overload's. Common Lisp passes a reference already, so the by-value form is the whole contract, and projecting the ref form would be a second name for one operation.",
    "TransformNormal(Vector3&,Matrix&,Vector3&)": "A by-reference overload of a pure computation. It exists in XNA so a caller can avoid copying a value type into a call and can write the answer into storage it already has; the value it computes is the by-value overload's. Common Lisp passes a reference already, so the by-value form is the whole contract, and projecting the ref form would be a second name for one operation.",
    "Transform(Vector3&,Quaternion&,Vector3&)": "A by-reference overload of a pure computation. It exists in XNA so a caller can avoid copying a value type into a call and can write the answer into storage it already has; the value it computes is the by-value overload's. Common Lisp passes a reference already, so the by-value form is the whole contract, and projecting the ref form would be a second name for one operation."
   },
   "unimplemented": {},
   "overload_families": {
    "Equals": {
     "lisp": "vector3-equal",
     "reason": "Common Lisp has no operator overloading."
    },
    "Multiply": {
     "lisp": "vector3-multiply",
     "reason": "One generic function with methods on REAL and on the vector type."
    },
    "Divide": {
     "lisp": "vector3-divide",
     "reason": "As Multiply."
    },
    "op_Multiply": {
     "lisp": "vector3-multiply",
     "reason": "As Multiply."
    },
    "op_Division": {
     "lisp": "vector3-divide",
     "reason": "As Multiply."
    },
    "Normalize": {
     "lisp": "vector3-normalize and vector3-normalized",
     "reason": "The instance form mutates; the static form does not."
    },
    ".ctor": {
     "lisp": "make-vector3 and make-vector3-from-vector2",
     "reason": "The constructor family splits on what its first argument is, and Common Lisp cannot dispatch a constructor on that without making the whole family one function that guesses. Each shape gets a named maker instead."
    },
    "Transform": {
     "lisp": "vector3-transform and vector3-transform-array",
     "reason": "The three-argument whole-array overload and the six-argument windowed one differ only by trailing parameters, so one function with :SOURCE-INDEX, :DESTINATION-INDEX and :LENGTH expresses both. The Matrix and Quaternion forms are methods of the element transform, dispatching on the second argument."
    },
    "TransformNormal": {
     "lisp": "vector3-transform-normal and vector3-transform-normal-array",
     "reason": "The three-argument whole-array overload and the six-argument windowed one differ only by trailing parameters, so one function with :SOURCE-INDEX, :DESTINATION-INDEX and :LENGTH expresses both. The Matrix and Quaternion forms are methods of the element transform, dispatching on the second argument."
    }
   },
   "by_reference_reason": "A by-reference overload of a pure computation. It exists in XNA so a caller can avoid copying a value type into a call and can write the answer into storage it already has; the value it computes is the by-value overload's. Common Lisp passes a reference already, so the by-value form is the whole contract, and projecting the ref form would be a second name for one operation."
  },
  "Microsoft.Xna.Framework.Vector4": {
   "lisp_package": "microsoft.xna.framework",
   "lisp_name": "vector4",
   "lisp_kind": "structure",
   "constructors": {
    "new(Single,Single,Single,Single)": "make-vector4",
    "new(Single)": "make-vector4",
    "new(Vector2,Single,Single)": "make-vector4-from-vector2",
    "new(Vector3,Single)": "make-vector4-from-vector3"
   },
   "member_overrides": {
    "Equals(Vector4)": {
     "lisp": "vector4-equal"
    },
    "op_Equality(Vector4,Vector4)": {
     "lisp": "vector4-equal"
    },
    "op_Addition(Vector4,Vector4)": {
     "lisp": "vector4-add"
    },
    "op_Subtraction(Vector4,Vector4)": {
     "lisp": "vector4-subtract"
    },
    "op_UnaryNegation(Vector4)": {
     "lisp": "vector4-negate"
    },
    "op_Multiply(Vector4,Single)": {
     "lisp": "vector4-multiply",
     "generic": true
    },
    "op_Multiply(Single,Vector4)": {
     "lisp": "vector4-multiply",
     "generic": true
    },
    "op_Multiply(Vector4,Vector4)": {
     "lisp": "vector4-multiply",
     "generic": true
    },
    "op_Division(Vector4,Single)": {
     "lisp": "vector4-divide",
     "generic": true
    },
    "op_Division(Vector4,Vector4)": {
     "lisp": "vector4-divide",
     "generic": true
    },
    "Add(Vector4,Vector4)": {
     "lisp": "vector4-add"
    },
    "Subtract(Vector4,Vector4)": {
     "lisp": "vector4-subtract"
    },
    "Negate(Vector4)": {
     "lisp": "vector4-negate"
    },
    "Multiply(Vector4,Single)": {
     "lisp": "vector4-multiply",
     "generic": true
    },
    "Multiply(Vector4,Vector4)": {
     "lisp": "vector4-multiply",
     "generic": true
    },
    "Divide(Vector4,Single)": {
     "lisp": "vector4-divide",
     "generic": true
    },
    "Divide(Vector4,Vector4)": {
     "lisp": "vector4-divide",
     "generic": true
    },
    "Distance(Vector4,Vector4)": {
     "lisp": "vector4-distance"
    },
    "DistanceSquared(Vector4,Vector4)": {
     "lisp": "vector4-distance-squared"
    },
    "Dot(Vector4,Vector4)": {
     "lisp": "vector4-dot"
    },
    "Min(Vector4,Vector4)": {
     "lisp": "vector4-min"
    },
    "Max(Vector4,Vector4)": {
     "lisp": "vector4-max"
    },
    "Clamp(Vector4,Vector4,Vector4)": {
     "lisp": "vector4-clamp"
    },
    "Lerp(Vector4,Vector4,Single)": {
     "lisp": "vector4-lerp"
    },
    "SmoothStep(Vector4,Vector4,Single)": {
     "lisp": "vector4-smooth-step"
    },
    "Barycentric(Vector4,Vector4,Vector4,Single,Single)": {
     "lisp": "vector4-barycentric"
    },
    "CatmullRom(Vector4,Vector4,Vector4,Vector4,Single)": {
     "lisp": "vector4-catmull-rom"
    },
    "Hermite(Vector4,Vector4,Vector4,Vector4,Single)": {
     "lisp": "vector4-hermite"
    },
    "Normalize()": {
     "lisp": "vector4-normalize"
    },
    "Normalize(Vector4)": {
     "lisp": "vector4-normalized",
     "reason": "The instance Normalize mutates and the static one answers a new vector. One Lisp function cannot be both without the caller having to know which it got, so the static overload takes the adjective."
    },
    "Length()": {
     "lisp": "vector4-length"
    },
    "LengthSquared()": {
     "lisp": "vector4-length-squared"
    },
    "Transform(Vector2,Matrix)": {
     "lisp": "vector4-transform",
     "generic": true
    },
    "Transform(Vector3,Matrix)": {
     "lisp": "vector4-transform",
     "generic": true
    },
    "Transform(Vector4,Matrix)": {
     "lisp": "vector4-transform",
     "generic": true
    },
    "Transform(Vector2,Quaternion)": {
     "lisp": "vector4-transform",
     "generic": true
    },
    "Transform(Vector3,Quaternion)": {
     "lisp": "vector4-transform",
     "generic": true
    },
    "Transform(Vector4,Quaternion)": {
     "lisp": "vector4-transform",
     "generic": true
    },
    "Transform(Vector4[],Matrix&,Vector4[])": {
     "lisp": "vector4-transform-array",
     "reason": "The array overloads: SOURCE-INDEX, DESTINATION-INDEX and LENGTH express the windowed form, and the whole-array form is the same function with none of them."
    },
    "Transform(Vector4[],Int32,Matrix&,Vector4[],Int32,Int32)": {
     "lisp": "vector4-transform-array",
     "reason": "The array overloads: SOURCE-INDEX, DESTINATION-INDEX and LENGTH express the windowed form, and the whole-array form is the same function with none of them."
    },
    "Transform(Vector4[],Quaternion&,Vector4[])": {
     "lisp": "vector4-transform-array",
     "reason": "The array overloads: SOURCE-INDEX, DESTINATION-INDEX and LENGTH express the windowed form, and the whole-array form is the same function with none of them."
    },
    "Transform(Vector4[],Int32,Quaternion&,Vector4[],Int32,Int32)": {
     "lisp": "vector4-transform-array",
     "reason": "The array overloads: SOURCE-INDEX, DESTINATION-INDEX and LENGTH express the windowed form, and the whole-array form is the same function with none of them."
    }
   },
   "not_applicable": {
    "Equals(Object)": "There is no universal Object to compare against.",
    "Distance(Vector4&,Vector4&,Single&)": "A by-reference overload of a pure computation. It exists in XNA so a caller can avoid copying a value type into a call and can write the answer into storage it already has; the value it computes is the by-value overload's. Common Lisp passes a reference already, so the by-value form is the whole contract, and projecting the ref form would be a second name for one operation.",
    "DistanceSquared(Vector4&,Vector4&,Single&)": "A by-reference overload of a pure computation. It exists in XNA so a caller can avoid copying a value type into a call and can write the answer into storage it already has; the value it computes is the by-value overload's. Common Lisp passes a reference already, so the by-value form is the whole contract, and projecting the ref form would be a second name for one operation.",
    "Dot(Vector4&,Vector4&,Single&)": "A by-reference overload of a pure computation. It exists in XNA so a caller can avoid copying a value type into a call and can write the answer into storage it already has; the value it computes is the by-value overload's. Common Lisp passes a reference already, so the by-value form is the whole contract, and projecting the ref form would be a second name for one operation.",
    "Normalize(Vector4&,Vector4&)": "A by-reference overload of a pure computation. It exists in XNA so a caller can avoid copying a value type into a call and can write the answer into storage it already has; the value it computes is the by-value overload's. Common Lisp passes a reference already, so the by-value form is the whole contract, and projecting the ref form would be a second name for one operation.",
    "Min(Vector4&,Vector4&,Vector4&)": "A by-reference overload of a pure computation. It exists in XNA so a caller can avoid copying a value type into a call and can write the answer into storage it already has; the value it computes is the by-value overload's. Common Lisp passes a reference already, so the by-value form is the whole contract, and projecting the ref form would be a second name for one operation.",
    "Max(Vector4&,Vector4&,Vector4&)": "A by-reference overload of a pure computation. It exists in XNA so a caller can avoid copying a value type into a call and can write the answer into storage it already has; the value it computes is the by-value overload's. Common Lisp passes a reference already, so the by-value form is the whole contract, and projecting the ref form would be a second name for one operation.",
    "Clamp(Vector4&,Vector4&,Vector4&,Vector4&)": "A by-reference overload of a pure computation. It exists in XNA so a caller can avoid copying a value type into a call and can write the answer into storage it already has; the value it computes is the by-value overload's. Common Lisp passes a reference already, so the by-value form is the whole contract, and projecting the ref form would be a second name for one operation.",
    "Lerp(Vector4&,Vector4&,Single,Vector4&)": "A by-reference overload of a pure computation. It exists in XNA so a caller can avoid copying a value type into a call and can write the answer into storage it already has; the value it computes is the by-value overload's. Common Lisp passes a reference already, so the by-value form is the whole contract, and projecting the ref form would be a second name for one operation.",
    "SmoothStep(Vector4&,Vector4&,Single,Vector4&)": "A by-reference overload of a pure computation. It exists in XNA so a caller can avoid copying a value type into a call and can write the answer into storage it already has; the value it computes is the by-value overload's. Common Lisp passes a reference already, so the by-value form is the whole contract, and projecting the ref form would be a second name for one operation.",
    "Barycentric(Vector4&,Vector4&,Vector4&,Single,Single,Vector4&)": "A by-reference overload of a pure computation. It exists in XNA so a caller can avoid copying a value type into a call and can write the answer into storage it already has; the value it computes is the by-value overload's. Common Lisp passes a reference already, so the by-value form is the whole contract, and projecting the ref form would be a second name for one operation.",
    "CatmullRom(Vector4&,Vector4&,Vector4&,Vector4&,Single,Vector4&)": "A by-reference overload of a pure computation. It exists in XNA so a caller can avoid copying a value type into a call and can write the answer into storage it already has; the value it computes is the by-value overload's. Common Lisp passes a reference already, so the by-value form is the whole contract, and projecting the ref form would be a second name for one operation.",
    "Hermite(Vector4&,Vector4&,Vector4&,Vector4&,Single,Vector4&)": "A by-reference overload of a pure computation. It exists in XNA so a caller can avoid copying a value type into a call and can write the answer into storage it already has; the value it computes is the by-value overload's. Common Lisp passes a reference already, so the by-value form is the whole contract, and projecting the ref form would be a second name for one operation.",
    "Negate(Vector4&,Vector4&)": "A by-reference overload of a pure computation. It exists in XNA so a caller can avoid copying a value type into a call and can write the answer into storage it already has; the value it computes is the by-value overload's. Common Lisp passes a reference already, so the by-value form is the whole contract, and projecting the ref form would be a second name for one operation.",
    "Add(Vector4&,Vector4&,Vector4&)": "A by-reference overload of a pure computation. It exists in XNA so a caller can avoid copying a value type into a call and can write the answer into storage it already has; the value it computes is the by-value overload's. Common Lisp passes a reference already, so the by-value form is the whole contract, and projecting the ref form would be a second name for one operation.",
    "Subtract(Vector4&,Vector4&,Vector4&)": "A by-reference overload of a pure computation. It exists in XNA so a caller can avoid copying a value type into a call and can write the answer into storage it already has; the value it computes is the by-value overload's. Common Lisp passes a reference already, so the by-value form is the whole contract, and projecting the ref form would be a second name for one operation.",
    "Multiply(Vector4&,Vector4&,Vector4&)": "A by-reference overload of a pure computation. It exists in XNA so a caller can avoid copying a value type into a call and can write the answer into storage it already has; the value it computes is the by-value overload's. Common Lisp passes a reference already, so the by-value form is the whole contract, and projecting the ref form would be a second name for one operation.",
    "Multiply(Vector4&,Single,Vector4&)": "A by-reference overload of a pure computation. It exists in XNA so a caller can avoid copying a value type into a call and can write the answer into storage it already has; the value it computes is the by-value overload's. Common Lisp passes a reference already, so the by-value form is the whole contract, and projecting the ref form would be a second name for one operation.",
    "Divide(Vector4&,Vector4&,Vector4&)": "A by-reference overload of a pure computation. It exists in XNA so a caller can avoid copying a value type into a call and can write the answer into storage it already has; the value it computes is the by-value overload's. Common Lisp passes a reference already, so the by-value form is the whole contract, and projecting the ref form would be a second name for one operation.",
    "Divide(Vector4&,Single,Vector4&)": "A by-reference overload of a pure computation. It exists in XNA so a caller can avoid copying a value type into a call and can write the answer into storage it already has; the value it computes is the by-value overload's. Common Lisp passes a reference already, so the by-value form is the whole contract, and projecting the ref form would be a second name for one operation.",
    "Transform(Vector2&,Matrix&,Vector4&)": "A by-reference overload of a pure computation. It exists in XNA so a caller can avoid copying a value type into a call and can write the answer into storage it already has; the value it computes is the by-value overload's. Common Lisp passes a reference already, so the by-value form is the whole contract, and projecting the ref form would be a second name for one operation.",
    "Transform(Vector3&,Matrix&,Vector4&)": "A by-reference overload of a pure computation. It exists in XNA so a caller can avoid copying a value type into a call and can write the answer into storage it already has; the value it computes is the by-value overload's. Common Lisp passes a reference already, so the by-value form is the whole contract, and projecting the ref form would be a second name for one operation.",
    "Transform(Vector4&,Matrix&,Vector4&)": "A by-reference overload of a pure computation. It exists in XNA so a caller can avoid copying a value type into a call and can write the answer into storage it already has; the value it computes is the by-value overload's. Common Lisp passes a reference already, so the by-value form is the whole contract, and projecting the ref form would be a second name for one operation.",
    "Transform(Vector2&,Quaternion&,Vector4&)": "A by-reference overload of a pure computation. It exists in XNA so a caller can avoid copying a value type into a call and can write the answer into storage it already has; the value it computes is the by-value overload's. Common Lisp passes a reference already, so the by-value form is the whole contract, and projecting the ref form would be a second name for one operation.",
    "Transform(Vector3&,Quaternion&,Vector4&)": "A by-reference overload of a pure computation. It exists in XNA so a caller can avoid copying a value type into a call and can write the answer into storage it already has; the value it computes is the by-value overload's. Common Lisp passes a reference already, so the by-value form is the whole contract, and projecting the ref form would be a second name for one operation.",
    "Transform(Vector4&,Quaternion&,Vector4&)": "A by-reference overload of a pure computation. It exists in XNA so a caller can avoid copying a value type into a call and can write the answer into storage it already has; the value it computes is the by-value overload's. Common Lisp passes a reference already, so the by-value form is the whole contract, and projecting the ref form would be a second name for one operation."
   },
   "unimplemented": {},
   "overload_families": {
    "Equals": {
     "lisp": "vector4-equal",
     "reason": "Common Lisp has no operator overloading."
    },
    "Multiply": {
     "lisp": "vector4-multiply",
     "reason": "One generic function with methods on REAL and on the vector type."
    },
    "Divide": {
     "lisp": "vector4-divide",
     "reason": "As Multiply."
    },
    "op_Multiply": {
     "lisp": "vector4-multiply",
     "reason": "As Multiply."
    },
    "op_Division": {
     "lisp": "vector4-divide",
     "reason": "As Multiply."
    },
    "Normalize": {
     "lisp": "vector4-normalize and vector4-normalized",
     "reason": "The instance form mutates; the static form does not."
    },
    ".ctor": {
     "lisp": "make-vector4, make-vector4-from-vector2 and make-vector4-from-vector3",
     "reason": "The constructor family splits on what its first argument is, and Common Lisp cannot dispatch a constructor on that without making the whole family one function that guesses. Each shape gets a named maker instead."
    },
    "Transform": {
     "lisp": "vector4-transform and vector4-transform-array",
     "reason": "The three-argument whole-array overload and the six-argument windowed one differ only by trailing parameters, so one function with :SOURCE-INDEX, :DESTINATION-INDEX and :LENGTH expresses both. The Matrix and Quaternion forms are methods of the element transform, dispatching on the second argument."
    },
    "TransformNormal": {
     "lisp": "vector4-transform-normal and vector4-transform-normal-array",
     "reason": "The three-argument whole-array overload and the six-argument windowed one differ only by trailing parameters, so one function with :SOURCE-INDEX, :DESTINATION-INDEX and :LENGTH expresses both. The Matrix and Quaternion forms are methods of the element transform, dispatching on the second argument."
    }
   },
   "by_reference_reason": "A by-reference overload of a pure computation. It exists in XNA so a caller can avoid copying a value type into a call and can write the answer into storage it already has; the value it computes is the by-value overload's. Common Lisp passes a reference already, so the by-value form is the whole contract, and projecting the ref form would be a second name for one operation."
  },
  "Microsoft.Xna.Framework.Quaternion": {
   "lisp_package": "microsoft.xna.framework",
   "lisp_name": "quaternion",
   "lisp_kind": "structure",
   "constructors": {
    "new(Single,Single,Single,Single)": "make-quaternion",
    "new(Vector3,Single)": "make-quaternion-from-vector3"
   },
   "member_overrides": {
    "Equals(Quaternion)": {
     "lisp": "quaternion-equal"
    },
    "op_Equality(Quaternion,Quaternion)": {
     "lisp": "quaternion-equal"
    },
    "op_Addition(Quaternion,Quaternion)": {
     "lisp": "quaternion-add"
    },
    "op_Subtraction(Quaternion,Quaternion)": {
     "lisp": "quaternion-subtract"
    },
    "op_UnaryNegation(Quaternion)": {
     "lisp": "quaternion-negate"
    },
    "op_Multiply(Quaternion,Quaternion)": {
     "lisp": "quaternion-multiply",
     "generic": true
    },
    "op_Multiply(Quaternion,Single)": {
     "lisp": "quaternion-multiply",
     "generic": true
    },
    "op_Division(Quaternion,Quaternion)": {
     "lisp": "quaternion-divide"
    },
    "Add(Quaternion,Quaternion)": {
     "lisp": "quaternion-add"
    },
    "Subtract(Quaternion,Quaternion)": {
     "lisp": "quaternion-subtract"
    },
    "Negate(Quaternion)": {
     "lisp": "quaternion-negate"
    },
    "Multiply(Quaternion,Quaternion)": {
     "lisp": "quaternion-multiply",
     "generic": true
    },
    "Multiply(Quaternion,Single)": {
     "lisp": "quaternion-multiply",
     "generic": true
    },
    "Divide(Quaternion,Quaternion)": {
     "lisp": "quaternion-divide"
    },
    "Dot(Quaternion,Quaternion)": {
     "lisp": "quaternion-dot"
    },
    "Concatenate(Quaternion,Quaternion)": {
     "lisp": "quaternion-concatenate"
    },
    "Lerp(Quaternion,Quaternion,Single)": {
     "lisp": "quaternion-lerp"
    },
    "Slerp(Quaternion,Quaternion,Single)": {
     "lisp": "quaternion-slerp"
    },
    "Inverse(Quaternion)": {
     "lisp": "quaternion-inverse"
    },
    "Normalize()": {
     "lisp": "quaternion-normalize"
    },
    "Normalize(Quaternion)": {
     "lisp": "quaternion-normalized",
     "reason": "The instance Normalize mutates and the static one answers a new value."
    },
    "Conjugate()": {
     "lisp": "quaternion-conjugate"
    },
    "Conjugate(Quaternion)": {
     "lisp": "quaternion-conjugated",
     "reason": "The instance Conjugate mutates and the static one answers a new value."
    },
    "Length()": {
     "lisp": "quaternion-length"
    },
    "LengthSquared()": {
     "lisp": "quaternion-length-squared"
    },
    "CreateFromAxisAngle(Vector3,Single)": {
     "lisp": "quaternion-create-from-axis-angle"
    },
    "CreateFromYawPitchRoll(Single,Single,Single)": {
     "lisp": "quaternion-create-from-yaw-pitch-roll"
    },
    "CreateFromRotationMatrix(Matrix)": {
     "lisp": "quaternion-create-from-rotation-matrix"
    },
    "Identity": {
     "lisp": "quaternion-identity"
    }
   },
   "not_applicable": {
    "Equals(Object)": "There is no universal Object to compare against.",
    "Normalize(Quaternion&,Quaternion&)": "A by-reference overload of a pure computation. It exists in XNA so a caller can avoid copying a value type into a call and can write the answer into storage it already has; the value it computes is the by-value overload's. Common Lisp passes a reference already, so the by-value form is the whole contract, and projecting the ref form would be a second name for one operation.",
    "Conjugate(Quaternion&,Quaternion&)": "A by-reference overload of a pure computation. It exists in XNA so a caller can avoid copying a value type into a call and can write the answer into storage it already has; the value it computes is the by-value overload's. Common Lisp passes a reference already, so the by-value form is the whole contract, and projecting the ref form would be a second name for one operation.",
    "Inverse(Quaternion&,Quaternion&)": "A by-reference overload of a pure computation. It exists in XNA so a caller can avoid copying a value type into a call and can write the answer into storage it already has; the value it computes is the by-value overload's. Common Lisp passes a reference already, so the by-value form is the whole contract, and projecting the ref form would be a second name for one operation.",
    "CreateFromAxisAngle(Vector3&,Single,Quaternion&)": "A by-reference overload of a pure computation. It exists in XNA so a caller can avoid copying a value type into a call and can write the answer into storage it already has; the value it computes is the by-value overload's. Common Lisp passes a reference already, so the by-value form is the whole contract, and projecting the ref form would be a second name for one operation.",
    "CreateFromYawPitchRoll(Single,Single,Single,Quaternion&)": "A by-reference overload of a pure computation. It exists in XNA so a caller can avoid copying a value type into a call and can write the answer into storage it already has; the value it computes is the by-value overload's. Common Lisp passes a reference already, so the by-value form is the whole contract, and projecting the ref form would be a second name for one operation.",
    "CreateFromRotationMatrix(Matrix&,Quaternion&)": "A by-reference overload of a pure computation. It exists in XNA so a caller can avoid copying a value type into a call and can write the answer into storage it already has; the value it computes is the by-value overload's. Common Lisp passes a reference already, so the by-value form is the whole contract, and projecting the ref form would be a second name for one operation.",
    "Dot(Quaternion&,Quaternion&,Single&)": "A by-reference overload of a pure computation. It exists in XNA so a caller can avoid copying a value type into a call and can write the answer into storage it already has; the value it computes is the by-value overload's. Common Lisp passes a reference already, so the by-value form is the whole contract, and projecting the ref form would be a second name for one operation.",
    "Slerp(Quaternion&,Quaternion&,Single,Quaternion&)": "A by-reference overload of a pure computation. It exists in XNA so a caller can avoid copying a value type into a call and can write the answer into storage it already has; the value it computes is the by-value overload's. Common Lisp passes a reference already, so the by-value form is the whole contract, and projecting the ref form would be a second name for one operation.",
    "Lerp(Quaternion&,Quaternion&,Single,Quaternion&)": "A by-reference overload of a pure computation. It exists in XNA so a caller can avoid copying a value type into a call and can write the answer into storage it already has; the value it computes is the by-value overload's. Common Lisp passes a reference already, so the by-value form is the whole contract, and projecting the ref form would be a second name for one operation.",
    "Concatenate(Quaternion&,Quaternion&,Quaternion&)": "A by-reference overload of a pure computation. It exists in XNA so a caller can avoid copying a value type into a call and can write the answer into storage it already has; the value it computes is the by-value overload's. Common Lisp passes a reference already, so the by-value form is the whole contract, and projecting the ref form would be a second name for one operation.",
    "Negate(Quaternion&,Quaternion&)": "A by-reference overload of a pure computation. It exists in XNA so a caller can avoid copying a value type into a call and can write the answer into storage it already has; the value it computes is the by-value overload's. Common Lisp passes a reference already, so the by-value form is the whole contract, and projecting the ref form would be a second name for one operation.",
    "Add(Quaternion&,Quaternion&,Quaternion&)": "A by-reference overload of a pure computation. It exists in XNA so a caller can avoid copying a value type into a call and can write the answer into storage it already has; the value it computes is the by-value overload's. Common Lisp passes a reference already, so the by-value form is the whole contract, and projecting the ref form would be a second name for one operation.",
    "Subtract(Quaternion&,Quaternion&,Quaternion&)": "A by-reference overload of a pure computation. It exists in XNA so a caller can avoid copying a value type into a call and can write the answer into storage it already has; the value it computes is the by-value overload's. Common Lisp passes a reference already, so the by-value form is the whole contract, and projecting the ref form would be a second name for one operation.",
    "Multiply(Quaternion&,Quaternion&,Quaternion&)": "A by-reference overload of a pure computation. It exists in XNA so a caller can avoid copying a value type into a call and can write the answer into storage it already has; the value it computes is the by-value overload's. Common Lisp passes a reference already, so the by-value form is the whole contract, and projecting the ref form would be a second name for one operation.",
    "Multiply(Quaternion&,Single,Quaternion&)": "A by-reference overload of a pure computation. It exists in XNA so a caller can avoid copying a value type into a call and can write the answer into storage it already has; the value it computes is the by-value overload's. Common Lisp passes a reference already, so the by-value form is the whole contract, and projecting the ref form would be a second name for one operation.",
    "Divide(Quaternion&,Quaternion&,Quaternion&)": "A by-reference overload of a pure computation. It exists in XNA so a caller can avoid copying a value type into a call and can write the answer into storage it already has; the value it computes is the by-value overload's. Common Lisp passes a reference already, so the by-value form is the whole contract, and projecting the ref form would be a second name for one operation."
   },
   "unimplemented": {},
   "by_reference_reason": "A by-reference overload of a pure computation. It exists in XNA so a caller can avoid copying a value type into a call and can write the answer into storage it already has; the value it computes is the by-value overload's. Common Lisp passes a reference already, so the by-value form is the whole contract, and projecting the ref form would be a second name for one operation.",
   "overload_families": {
    "Equals": {
     "lisp": "quaternion-equal",
     "reason": "Common Lisp has no operator overloading."
    },
    "Multiply": {
     "lisp": "quaternion-multiply",
     "reason": "One generic function with methods on QUATERNION and on REAL."
    },
    "op_Multiply": {
     "lisp": "quaternion-multiply",
     "reason": "As Multiply."
    },
    "Normalize": {
     "lisp": "quaternion-normalize and quaternion-normalized",
     "reason": "The instance form mutates; the static form does not."
    },
    "Conjugate": {
     "lisp": "quaternion-conjugate and quaternion-conjugated",
     "reason": "The instance form mutates; the static form does not."
    },
    ".ctor": {
     "lisp": "make-quaternion and make-quaternion-from-vector3",
     "reason": "The constructor family splits on its first argument's type."
    }
   }
  },
  "Microsoft.Xna.Framework.Matrix": {
   "lisp_package": "microsoft.xna.framework",
   "lisp_name": "matrix",
   "lisp_kind": "structure",
   "constructors": {
    "new(Single,Single,Single,Single,Single,Single,Single,Single,Single,Single,Single,Single,Single,Single,Single,Single)": "make-matrix"
   },
   "member_overrides": {
    "Equals(Matrix)": {
     "lisp": "matrix-equal"
    },
    "op_Equality(Matrix,Matrix)": {
     "lisp": "matrix-equal"
    },
    "op_Addition(Matrix,Matrix)": {
     "lisp": "matrix-add"
    },
    "op_Subtraction(Matrix,Matrix)": {
     "lisp": "matrix-subtract"
    },
    "op_UnaryNegation(Matrix)": {
     "lisp": "matrix-negate"
    },
    "op_Multiply(Matrix,Matrix)": {
     "lisp": "matrix-multiply",
     "generic": true
    },
    "op_Multiply(Matrix,Single)": {
     "lisp": "matrix-multiply",
     "generic": true
    },
    "op_Multiply(Single,Matrix)": {
     "lisp": "matrix-multiply",
     "generic": true
    },
    "op_Division(Matrix,Matrix)": {
     "lisp": "matrix-divide",
     "generic": true
    },
    "op_Division(Matrix,Single)": {
     "lisp": "matrix-divide",
     "generic": true
    },
    "Add(Matrix,Matrix)": {
     "lisp": "matrix-add"
    },
    "Subtract(Matrix,Matrix)": {
     "lisp": "matrix-subtract"
    },
    "Negate(Matrix)": {
     "lisp": "matrix-negate"
    },
    "Multiply(Matrix,Matrix)": {
     "lisp": "matrix-multiply",
     "generic": true
    },
    "Multiply(Matrix,Single)": {
     "lisp": "matrix-multiply",
     "generic": true
    },
    "Divide(Matrix,Matrix)": {
     "lisp": "matrix-divide",
     "generic": true
    },
    "Divide(Matrix,Single)": {
     "lisp": "matrix-divide",
     "generic": true
    },
    "Transpose(Matrix)": {
     "lisp": "matrix-transpose"
    },
    "Determinant()": {
     "lisp": "matrix-determinant"
    },
    "Invert(Matrix)": {
     "lisp": "matrix-invert"
    },
    "Lerp(Matrix,Matrix,Single)": {
     "lisp": "matrix-lerp"
    },
    "Transform(Matrix,Quaternion)": {
     "lisp": "matrix-transform"
    },
    "CreateTranslation(Vector3)": {
     "lisp": "matrix-create-translation",
     "generic": true
    },
    "CreateTranslation(Single,Single,Single)": {
     "lisp": "matrix-create-translation",
     "generic": true
    },
    "CreateScale(Vector3)": {
     "lisp": "matrix-create-scale",
     "generic": true
    },
    "CreateScale(Single,Single,Single)": {
     "lisp": "matrix-create-scale",
     "generic": true
    },
    "CreateScale(Single)": {
     "lisp": "matrix-create-scale",
     "generic": true
    },
    "CreateRotationX(Single)": {
     "lisp": "matrix-create-rotation-x"
    },
    "CreateRotationY(Single)": {
     "lisp": "matrix-create-rotation-y"
    },
    "CreateRotationZ(Single)": {
     "lisp": "matrix-create-rotation-z"
    },
    "CreateFromAxisAngle(Vector3,Single)": {
     "lisp": "matrix-create-from-axis-angle"
    },
    "CreateFromQuaternion(Quaternion)": {
     "lisp": "matrix-create-from-quaternion"
    },
    "CreateFromYawPitchRoll(Single,Single,Single)": {
     "lisp": "matrix-create-from-yaw-pitch-roll"
    },
    "CreatePerspectiveFieldOfView(Single,Single,Single,Single)": {
     "lisp": "matrix-create-perspective-field-of-view"
    },
    "CreatePerspective(Single,Single,Single,Single)": {
     "lisp": "matrix-create-perspective"
    },
    "CreatePerspectiveOffCenter(Single,Single,Single,Single,Single,Single)": {
     "lisp": "matrix-create-perspective-off-center"
    },
    "CreateOrthographic(Single,Single,Single,Single)": {
     "lisp": "matrix-create-orthographic"
    },
    "CreateOrthographicOffCenter(Single,Single,Single,Single,Single,Single)": {
     "lisp": "matrix-create-orthographic-off-center"
    },
    "CreateLookAt(Vector3,Vector3,Vector3)": {
     "lisp": "matrix-create-look-at"
    },
    "CreateWorld(Vector3,Vector3,Vector3)": {
     "lisp": "matrix-create-world"
    },
    "CreateBillboard(Vector3,Vector3,Vector3,Vector3])": {
     "lisp": "matrix-create-billboard",
     "reason": "The Vector3? camera-forward argument is an optional argument here; NIL is the absent value."
    },
    "Identity": {
     "lisp": "matrix-identity"
    },
    "Up": {
     "lisp": "matrix-up"
    },
    "Down": {
     "lisp": "matrix-down"
    },
    "Right": {
     "lisp": "matrix-right"
    },
    "Left": {
     "lisp": "matrix-left"
    },
    "Forward": {
     "lisp": "matrix-forward"
    },
    "Backward": {
     "lisp": "matrix-backward"
    },
    "Translation": {
     "lisp": "matrix-translation"
    },
    "M11": {
     "lisp": "matrix-m11"
    },
    "M12": {
     "lisp": "matrix-m12"
    },
    "M13": {
     "lisp": "matrix-m13"
    },
    "M14": {
     "lisp": "matrix-m14"
    },
    "M21": {
     "lisp": "matrix-m21"
    },
    "M22": {
     "lisp": "matrix-m22"
    },
    "M23": {
     "lisp": "matrix-m23"
    },
    "M24": {
     "lisp": "matrix-m24"
    },
    "M31": {
     "lisp": "matrix-m31"
    },
    "M32": {
     "lisp": "matrix-m32"
    },
    "M33": {
     "lisp": "matrix-m33"
    },
    "M34": {
     "lisp": "matrix-m34"
    },
    "M41": {
     "lisp": "matrix-m41"
    },
    "M42": {
     "lisp": "matrix-m42"
    },
    "M43": {
     "lisp": "matrix-m43"
    },
    "M44": {
     "lisp": "matrix-m44"
    }
   },
   "not_applicable": {
    "Equals(Object)": "There is no universal Object to compare against.",
    "Transpose(Matrix&,Matrix&)": "A by-reference overload of a pure computation. It exists in XNA so a caller can avoid copying a value type into a call and can write the answer into storage it already has; the value it computes is the by-value overload's. Common Lisp passes a reference already, so the by-value form is the whole contract, and projecting the ref form would be a second name for one operation.",
    "Invert(Matrix&,Matrix&)": "A by-reference overload of a pure computation. It exists in XNA so a caller can avoid copying a value type into a call and can write the answer into storage it already has; the value it computes is the by-value overload's. Common Lisp passes a reference already, so the by-value form is the whole contract, and projecting the ref form would be a second name for one operation.",
    "Lerp(Matrix&,Matrix&,Single,Matrix&)": "A by-reference overload of a pure computation. It exists in XNA so a caller can avoid copying a value type into a call and can write the answer into storage it already has; the value it computes is the by-value overload's. Common Lisp passes a reference already, so the by-value form is the whole contract, and projecting the ref form would be a second name for one operation.",
    "Negate(Matrix&,Matrix&)": "A by-reference overload of a pure computation. It exists in XNA so a caller can avoid copying a value type into a call and can write the answer into storage it already has; the value it computes is the by-value overload's. Common Lisp passes a reference already, so the by-value form is the whole contract, and projecting the ref form would be a second name for one operation.",
    "Add(Matrix&,Matrix&,Matrix&)": "A by-reference overload of a pure computation. It exists in XNA so a caller can avoid copying a value type into a call and can write the answer into storage it already has; the value it computes is the by-value overload's. Common Lisp passes a reference already, so the by-value form is the whole contract, and projecting the ref form would be a second name for one operation.",
    "Subtract(Matrix&,Matrix&,Matrix&)": "A by-reference overload of a pure computation. It exists in XNA so a caller can avoid copying a value type into a call and can write the answer into storage it already has; the value it computes is the by-value overload's. Common Lisp passes a reference already, so the by-value form is the whole contract, and projecting the ref form would be a second name for one operation.",
    "Multiply(Matrix&,Matrix&,Matrix&)": "A by-reference overload of a pure computation. It exists in XNA so a caller can avoid copying a value type into a call and can write the answer into storage it already has; the value it computes is the by-value overload's. Common Lisp passes a reference already, so the by-value form is the whole contract, and projecting the ref form would be a second name for one operation.",
    "Multiply(Matrix&,Single,Matrix&)": "A by-reference overload of a pure computation. It exists in XNA so a caller can avoid copying a value type into a call and can write the answer into storage it already has; the value it computes is the by-value overload's. Common Lisp passes a reference already, so the by-value form is the whole contract, and projecting the ref form would be a second name for one operation.",
    "Divide(Matrix&,Matrix&,Matrix&)": "A by-reference overload of a pure computation. It exists in XNA so a caller can avoid copying a value type into a call and can write the answer into storage it already has; the value it computes is the by-value overload's. Common Lisp passes a reference already, so the by-value form is the whole contract, and projecting the ref form would be a second name for one operation.",
    "Divide(Matrix&,Single,Matrix&)": "A by-reference overload of a pure computation. It exists in XNA so a caller can avoid copying a value type into a call and can write the answer into storage it already has; the value it computes is the by-value overload's. Common Lisp passes a reference already, so the by-value form is the whole contract, and projecting the ref form would be a second name for one operation.",
    "Transform(Matrix&,Quaternion&,Matrix&)": "A by-reference overload of a pure computation. It exists in XNA so a caller can avoid copying a value type into a call and can write the answer into storage it already has; the value it computes is the by-value overload's. Common Lisp passes a reference already, so the by-value form is the whole contract, and projecting the ref form would be a second name for one operation.",
    "CreateTranslation(Vector3&,Matrix&)": "A by-reference overload of a pure computation. It exists in XNA so a caller can avoid copying a value type into a call and can write the answer into storage it already has; the value it computes is the by-value overload's. Common Lisp passes a reference already, so the by-value form is the whole contract, and projecting the ref form would be a second name for one operation.",
    "CreateTranslation(Single,Single,Single,Matrix&)": "A by-reference overload of a pure computation. It exists in XNA so a caller can avoid copying a value type into a call and can write the answer into storage it already has; the value it computes is the by-value overload's. Common Lisp passes a reference already, so the by-value form is the whole contract, and projecting the ref form would be a second name for one operation.",
    "CreateScale(Vector3&,Matrix&)": "A by-reference overload of a pure computation. It exists in XNA so a caller can avoid copying a value type into a call and can write the answer into storage it already has; the value it computes is the by-value overload's. Common Lisp passes a reference already, so the by-value form is the whole contract, and projecting the ref form would be a second name for one operation.",
    "CreateScale(Single,Single,Single,Matrix&)": "A by-reference overload of a pure computation. It exists in XNA so a caller can avoid copying a value type into a call and can write the answer into storage it already has; the value it computes is the by-value overload's. Common Lisp passes a reference already, so the by-value form is the whole contract, and projecting the ref form would be a second name for one operation.",
    "CreateScale(Single,Matrix&)": "A by-reference overload of a pure computation. It exists in XNA so a caller can avoid copying a value type into a call and can write the answer into storage it already has; the value it computes is the by-value overload's. Common Lisp passes a reference already, so the by-value form is the whole contract, and projecting the ref form would be a second name for one operation.",
    "CreateRotationX(Single,Matrix&)": "A by-reference overload of a pure computation. It exists in XNA so a caller can avoid copying a value type into a call and can write the answer into storage it already has; the value it computes is the by-value overload's. Common Lisp passes a reference already, so the by-value form is the whole contract, and projecting the ref form would be a second name for one operation.",
    "CreateRotationY(Single,Matrix&)": "A by-reference overload of a pure computation. It exists in XNA so a caller can avoid copying a value type into a call and can write the answer into storage it already has; the value it computes is the by-value overload's. Common Lisp passes a reference already, so the by-value form is the whole contract, and projecting the ref form would be a second name for one operation.",
    "CreateRotationZ(Single,Matrix&)": "A by-reference overload of a pure computation. It exists in XNA so a caller can avoid copying a value type into a call and can write the answer into storage it already has; the value it computes is the by-value overload's. Common Lisp passes a reference already, so the by-value form is the whole contract, and projecting the ref form would be a second name for one operation.",
    "CreateFromAxisAngle(Vector3&,Single,Matrix&)": "A by-reference overload of a pure computation. It exists in XNA so a caller can avoid copying a value type into a call and can write the answer into storage it already has; the value it computes is the by-value overload's. Common Lisp passes a reference already, so the by-value form is the whole contract, and projecting the ref form would be a second name for one operation.",
    "CreateFromQuaternion(Quaternion&,Matrix&)": "A by-reference overload of a pure computation. It exists in XNA so a caller can avoid copying a value type into a call and can write the answer into storage it already has; the value it computes is the by-value overload's. Common Lisp passes a reference already, so the by-value form is the whole contract, and projecting the ref form would be a second name for one operation.",
    "CreateFromYawPitchRoll(Single,Single,Single,Matrix&)": "A by-reference overload of a pure computation. It exists in XNA so a caller can avoid copying a value type into a call and can write the answer into storage it already has; the value it computes is the by-value overload's. Common Lisp passes a reference already, so the by-value form is the whole contract, and projecting the ref form would be a second name for one operation.",
    "CreatePerspectiveFieldOfView(Single,Single,Single,Single,Matrix&)": "A by-reference overload of a pure computation. It exists in XNA so a caller can avoid copying a value type into a call and can write the answer into storage it already has; the value it computes is the by-value overload's. Common Lisp passes a reference already, so the by-value form is the whole contract, and projecting the ref form would be a second name for one operation.",
    "CreatePerspective(Single,Single,Single,Single,Matrix&)": "A by-reference overload of a pure computation. It exists in XNA so a caller can avoid copying a value type into a call and can write the answer into storage it already has; the value it computes is the by-value overload's. Common Lisp passes a reference already, so the by-value form is the whole contract, and projecting the ref form would be a second name for one operation.",
    "CreatePerspectiveOffCenter(Single,Single,Single,Single,Single,Single,Matrix&)": "A by-reference overload of a pure computation. It exists in XNA so a caller can avoid copying a value type into a call and can write the answer into storage it already has; the value it computes is the by-value overload's. Common Lisp passes a reference already, so the by-value form is the whole contract, and projecting the ref form would be a second name for one operation.",
    "CreateOrthographic(Single,Single,Single,Single,Matrix&)": "A by-reference overload of a pure computation. It exists in XNA so a caller can avoid copying a value type into a call and can write the answer into storage it already has; the value it computes is the by-value overload's. Common Lisp passes a reference already, so the by-value form is the whole contract, and projecting the ref form would be a second name for one operation.",
    "CreateOrthographicOffCenter(Single,Single,Single,Single,Single,Single,Matrix&)": "A by-reference overload of a pure computation. It exists in XNA so a caller can avoid copying a value type into a call and can write the answer into storage it already has; the value it computes is the by-value overload's. Common Lisp passes a reference already, so the by-value form is the whole contract, and projecting the ref form would be a second name for one operation.",
    "CreateLookAt(Vector3&,Vector3&,Vector3&,Matrix&)": "A by-reference overload of a pure computation. It exists in XNA so a caller can avoid copying a value type into a call and can write the answer into storage it already has; the value it computes is the by-value overload's. Common Lisp passes a reference already, so the by-value form is the whole contract, and projecting the ref form would be a second name for one operation.",
    "CreateWorld(Vector3&,Vector3&,Vector3&,Matrix&)": "A by-reference overload of a pure computation. It exists in XNA so a caller can avoid copying a value type into a call and can write the answer into storage it already has; the value it computes is the by-value overload's. Common Lisp passes a reference already, so the by-value form is the whole contract, and projecting the ref form would be a second name for one operation.",
    "CreateBillboard(Vector3&,Vector3&,Vector3&,Vector3],Matrix&)": "A by-reference overload of a pure computation. It exists in XNA so a caller can avoid copying a value type into a call and can write the answer into storage it already has; the value it computes is the by-value overload's. Common Lisp passes a reference already, so the by-value form is the whole contract, and projecting the ref form would be a second name for one operation."
   },
   "unimplemented": {
    "CreateShadow(Vector3,Plane)": "Needs Plane, which is not projected yet.",
    "CreateShadow(Vector3&,Plane&,Matrix&)": "Needs Plane, which is not projected yet.",
    "CreateReflection(Plane)": "Needs Plane, which is not projected yet.",
    "CreateReflection(Plane&,Matrix&)": "Needs Plane, which is not projected yet.",
    "CreateConstrainedBillboard(Vector3,Vector3,Vector3,Vector3],Vector3])": "The constrained billboard's degenerate-axis fallback is a chain of three nested thresholds against two optional vectors. It is deferred rather than approximated: an implementation that agreed on the ordinary case and diverged on the degenerate one would be worse than its absence.",
    "CreateConstrainedBillboard(Vector3&,Vector3&,Vector3&,Vector3],Vector3],Matrix&)": "See the by-value overload.",
    "Decompose(Vector3&,Quaternion&,Vector3&)": "Decompose is implemented with unsafe pointer arithmetic over a private CanonicalBasis and VectorBasis pair -- some 540 IL instructions with a fallback path for degenerate scales. Reproducing it faithfully is a separate piece of work, and an approximation that agreed on well-conditioned matrices and diverged on degenerate ones would be worse than its absence."
   },
   "by_reference_reason": "A by-reference overload of a pure computation. It exists in XNA so a caller can avoid copying a value type into a call and can write the answer into storage it already has; the value it computes is the by-value overload's. Common Lisp passes a reference already, so the by-value form is the whole contract, and projecting the ref form would be a second name for one operation.",
   "overload_families": {
    "Equals": {
     "lisp": "matrix-equal",
     "reason": "Common Lisp has no operator overloading."
    },
    "Multiply": {
     "lisp": "matrix-multiply",
     "reason": "One generic function with methods on MATRIX and on REAL."
    },
    "op_Multiply": {
     "lisp": "matrix-multiply",
     "reason": "As Multiply."
    },
    "Divide": {
     "lisp": "matrix-divide",
     "reason": "As Multiply."
    },
    "op_Division": {
     "lisp": "matrix-divide",
     "reason": "As Multiply."
    },
    "CreateTranslation": {
     "lisp": "matrix-create-translation",
     "reason": "One generic function with methods on VECTOR3 and on REAL; the three-float form uses the optional arguments."
    },
    "CreateScale": {
     "lisp": "matrix-create-scale",
     "reason": "As CreateTranslation, with the uniform form distinguished by supplying only one argument."
    },
    "CreateShadow": {
     "lisp": null,
     "reason": "Not projected: needs Plane."
    },
    "CreateReflection": {
     "lisp": null,
     "reason": "Not projected: needs Plane."
    },
    "CreateConstrainedBillboard": {
     "lisp": null,
     "reason": "Deferred; see the unimplemented entry."
    }
   },
   "field_naming_reason": "The M<row><column> fields keep their digits together: matrix-m11, not matrix-m-1-1. The identifier rule would split at the digit boundary and the result would be unreadable for the one type where the indices are the name."
  }
 }
}
